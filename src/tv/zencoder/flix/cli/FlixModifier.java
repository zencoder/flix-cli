package tv.zencoder.flix.cli;

/**
 * A child of a FlixBuilder.  A FlixModifier, not surprisingly, modifies
 * an existing Flix object (Filter, Codec, Muxer).
 * 
 * @author jdl
 *
 */
public interface FlixModifier extends OptionHandler {}
